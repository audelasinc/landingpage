DATABASE_URL="postgresql://YOUR_USER:YOUR_PASSWORD@localhost:5432/audelas?schema=public"
*(Replace `YOUR_USER` and `YOUR_PASSWORD` with your actual Postgres credentials).*

#### 2. Backend Installation & Seeding
Open your terminal in the `backend` folder:
```bash
# Install dependencies
npm install

# Create database tables
npx prisma db push

# Run the seed script to create users/programs
node seed.js

# Start the API server
node server.js
*Leave this terminal running.*

#### 3. Frontend Installation & Running
Open a **new** terminal in the `frontend` folder:
```bash
# Install dependencies
npm install

# Start the React app
npm run dev

#### 4. Interaction
Open your browser to `http://localhost:5173`.
* **Institution Login:** `admin_0@edu.com` / `password`
* **Student Login:** Check the output of the seed script for an email (e.g., `s0_xxxxx@audelas.com`) / `password`. Or simply Register a new student.